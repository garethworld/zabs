{% for link in linklists.main-menu.links %} {% if link.active or
    link.child_active %} {% else %} {% if link.type contains 'collection_link'
    %}
    <h2 class="{% render 'c-filter-h'%}">{{ link.title | escape }}</h2>
    {% endif %} {% endif %} {% endfor %}



    <!-- by age or -->
    {% for link in linklists.main-menu.links %} {% if link.levels >= 1 %}


    {% if link.active or link.child_active %}

 {% else %}
    <ul id="menu-{{ link.handle | escape }}" class="mt-auto mb-10 ">
      {% for sub_link in link.links %}

      <li>
        {% for tag in collection.tags %} {% if tag contains sub_link.title %}

 
        <a
         @click="cat"
          class="{% render 'c-filter-link'%} menu-opt {% if current_tags contains sub_link.title  %} menu-active underline{% endif %}"
          data-action = "/collections/{{ collection.handle }}/{{ sub_link.handle }}"
          >{{ sub_link.title | escape }}</a
        >
        {% endif %} {% endfor %}
      </li>
      {% endfor %}
    </ul>

    {% endif %} 
    {% endif %} {% endfor %}