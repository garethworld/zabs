<nav class='mr-4'>
  <ul>
    {% for link in linklists['right-menu'].links %}
      <li class=''>
        <a
          class='text-5 uppercase hover:text-tint3   {% if is_active %} text-tint3 {% endif %} {% render 'c-color-change' %}'
          href='{{ link.url }}'
        >
          {{- link.title | escape -}}
        </a>
      </li>
    {% endfor %}
  </ul>
</nav>
